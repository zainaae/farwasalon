/* Prices (and any interior) use the same Freedom Deal strip as home —
   one component, one alignment, one Azadi style. Kept as DealBanner
   export so existing imports keep working. */
export { default } from './deal-strip.jsx'
