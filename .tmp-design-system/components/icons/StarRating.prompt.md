Row of filled stars in one `<svg>` — used on review cards. Rule from the source: never show 5 filled stars next to a 4.6 rating; inline "4.6★" instead.

```jsx
<StarRating size={10} className="text-accent-gold" />
```
