One ruled row of the couture-style services menu: matted photo, Syne category name, stone tagline, count + availability, Unbounded "from Rs X" price.

```jsx
<MenuRow img="../../assets/threading.jpg" name="Threading" tagline="Precise brow & face threading from Rs 100" count={7} availability="Usually available same-day" fromPrice="Rs 100" popular />
```

Stack rows under a chapter header (gold `.section-title` + `.eyebrow` caption over a `border-bottom: 1px solid rgba(13,13,13,0.3)` rule).
