Mobile bottom action bar — Call / WhatsApp / Book (white Book segment) on a dark blurred rounded pill, with an optional live next-slot hint.

```jsx
<div style={{ position: 'fixed', left: 14, right: 14, bottom: 10 }}><StickyMobileCTA slotLabel="Today 3:00 PM" /></div>
```

Mobile-only in production (hidden ≥768px).
