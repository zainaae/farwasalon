Square-mat photo proof beside the real rating and volume line. **Below the fold only** — never the default under the home hero (use `ProofStrip` there).

```jsx
<ReviewProof images={['../../assets/bridal.jpg','../../assets/glow.jpg','../../assets/threading.jpg','../../assets/pedicure.jpg']} />
```

Mats are square (Farwa corners). Numbers must be real ("4.6★", "19 Google reviews") — never invented.
