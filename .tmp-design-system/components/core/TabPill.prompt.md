Category filter chip used on services, prices and blog — square, hairline border, uppercase.

```jsx
<TabPill active>All</TabPill>
<TabPill>Facials</TabPill>
```

Lay them in a horizontally-scrolling row on mobile (`display:flex; gap:0.5rem; overflow-x:auto`).
